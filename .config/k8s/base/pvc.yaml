# k8s/base/pvc.yaml
# Shared persistent volume

# machine learning model
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blog-model-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
